{
  "name": "core",
  "version": "1.0.0",
  "description": "Domínio compartilhado (TS puro, apenas uuid)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "import": "./dist/index.js", "types": "./dist/index.d.ts" } },
  "files": ["dist"],
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.json -w --preserveWatchOutput",
    "lint": "eslint --ext .ts src",
    "format": "prettier -w ."
  },
  "dependencies": {
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
